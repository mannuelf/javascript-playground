const Input = props => {
  return (
    <input
      data-test="component-input"
      type="text"
      name="guess"
      id="guess"
      className="form-control"
    />
  );
};

export default Input;
