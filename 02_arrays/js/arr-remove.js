const numbers = [1, 2, 3, 4];

// from the end
const end = numbers.pop(1, 2);
//console.log("removing", end);
//console.log(numbers);

// from the start
const first = numbers.shift();
console.log("removing:", first);
console.log(numbers);

// from the middle
numbers.splice();
